# Maple

一个单页网页形式的中文性格测试项目，主题是「你是上位者吗」。

## 项目内容

- `index.html`：完整网页文件，包含页面样式、题目逻辑和结果展示。
- 无需安装依赖，也不需要构建工具。

## 本地查看

直接双击打开 `index.html` 即可。

部分浏览器可能会限制本地文件行为。遇到显示异常时，可以在项目目录启动本地静态服务器：

```bash
python -m http.server 8000
```
